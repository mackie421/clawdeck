<% content_for :title, "Content Studio - clawdeck" %>
<% content_for :page_title, "Content Studio" %>

<div class="p-6" data-controller="content-studio">
  <%# Tab navigation %>
  <div class="flex items-center gap-1 mb-6 border-b border-border">
    <button data-content-studio-target="tab" data-action="click->content-studio#switchTab"
            data-tab="drafts"
            class="px-3 py-2 text-sm font-medium border-b-2 transition-colors cursor-pointer border-accent text-content">
      Drafts
      <% if @drafts.any? %>
        <span class="ml-1 text-xs bg-bg-elevated text-content-secondary px-1.5 py-0.5 rounded-full"><%= @drafts.size %></span>
      <% end %>
    </button>
    <button data-content-studio-target="tab" data-action="click->content-studio#switchTab"
            data-tab="ideas"
            class="px-3 py-2 text-sm font-medium border-b-2 transition-colors cursor-pointer border-transparent text-content-muted hover:text-content-secondary">
      Ideas
      <% if @ideas.any? %>
        <span class="ml-1 text-xs bg-bg-elevated text-content-secondary px-1.5 py-0.5 rounded-full"><%= @ideas.size %></span>
      <% end %>
    </button>
    <button data-content-studio-target="tab" data-action="click->content-studio#switchTab"
            data-tab="calendar"
            class="px-3 py-2 text-sm font-medium border-b-2 transition-colors cursor-pointer border-transparent text-content-muted hover:text-content-secondary">
      Calendar
    </button>
  </div>

  <%# Drafts panel %>
  <div data-content-studio-target="panel" data-panel="drafts">
    <%= render "contents/drafts_panel" %>
  </div>

  <%# Ideas panel %>
  <div data-content-studio-target="panel" data-panel="ideas" class="hidden">
    <%= render "contents/ideas_panel" %>
  </div>

  <%# Calendar panel %>
  <div data-content-studio-target="panel" data-panel="calendar" class="hidden">
    <%= render "contents/calendar_panel" %>
  </div>
</div>
