{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bin/railway-build.sh"
  },
  "deploy": {
    "startCommand": "echo DATABASE_URL=$DATABASE_URL | head -c 40 && bundle exec rails db:migrate && bundle exec puma -C config/puma.rb",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 120
  }
}
